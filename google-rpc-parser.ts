// import { createReader } from "./src/utils";

// export function parse(stream: ReadableStream<Uint8Array>) {
//   const reader = createReader(stream);
//   const buffer = new Uint8Array(4096);
//   const len = reader.read(buffer, 0, 30);
//   console.log(len);
//   reader.close();
//   return buffer;
// }
